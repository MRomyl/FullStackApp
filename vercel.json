{
  "builds": [
    { "src": "server/api.py", "use": "@vercel/python" },
    { "src": "client/package.json", "use": "@vercel/static-build", "config": { "distDir": "client/dist" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/api.py" },
    { "src": "/(.*)", "dest": "client/dist/$1" }
  ]
}
